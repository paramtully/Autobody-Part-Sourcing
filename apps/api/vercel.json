{
  "version": 2,
  "installCommand": "cd ../.. && npm ci --workspaces --include-workspace-root",
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "../../src/db/**",
          "../../src/ordering/**",
          "../../src/affiliate/**"
        ]
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.ts" }]
}
